db.driver=com.mysql.cj.jdbc.Driver
# Use MySQL or override the connection URL and driver for H2.
db.url=jdbc:mysql://localhost:3306/library_management?useSSL=false&serverTimezone=UTC
# Replace with your own MySQL username
db.username=root
# Replace with your own MySQL password
db.password=password
# Set db.initialize=true to auto-create schema when using H2 or a fresh database.
db.initialize=false
